DBMS  Database Manegment System
Database
Relationship: is how to connect data in a table to another data in another table.
1. one to one
2. one to many
3. many to many
ERD: Entity Relational Database
====================Normalization:
normal forms:

name tell sex address
=====================
hamdi		female	madina
hamdi		female	madina

un-normalized table:
student{name,tell,sex,birth_date,p_birth,class,responsible,address,...}
functional depencency: relationship b/w your primay key column and rest of other columns in a row.

id==>name,tell
id is determent
name is depedent

1NF:
1. atomic value: no column can take more than one value.
2. no repeating group of related data.
	student{name,tell,sex,birth_date,p_birth,class,...,....,...}
	responbislbe{res_name,res_tell}
	address{district,village,area}

2NF:
1. 1NF
2. setting Entity integrity & referential integrity

student{id(pk),name,tell,sex,birth_date,p_birth,..,add_no(fk), res_no(fk)}
		1 		ahmed 							grade1
		2 		Ayub
		3 		Miski
		4		Aisha
	responbislbe{res_no(pk),res_name,res_tell}
	address{add_no(pk),district,village,area}

3NF:
	1. 2NF
	2. no changible column
levels{lev_no(pk),lev_name,price}
		1 		d/hose 	10
		2 		d/dhexe 15
class{cl_no(pk),cl_name,lev_no(fk)}
		1 		grade1   1
		2 		grade2 	 1
		3 		grade3 	 1
BCNM: if there is many to many relationship for tables, midle table must exist.
	academic_year{y_no(pk),y_name,started,ended}
					1 		2022-2023
					2 		2023-2024
					3 		2024-2025
	student_class{sc_no(pk),id(fk),cl_no(fk),y_no(fk)}
					1 		1 		1 		 1
					2 		3 		1 		 1
					3 		2 		2 		 1
					4 		4 		2 		 2
					5    	1 		2 		 2

4NF:

1. responbislbe{res_no(pk),res_name,res_tell}
2. address{add_no(pk),district,village,area}
3. student{id(pk),name,tell,sex,birth_date,p_birth,..,add_no(fk), res_no(fk)}
4. levels{lev_no(pk),lev_name,price}
5. class{cl_no(pk),cl_name,lev_no(fk)}
6. academic_year{y_no(pk),y_name,started,ended}		
7. student_class{sc_no(pk),id(fk),cl_no(fk),y_no(fk)}